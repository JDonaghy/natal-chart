{
  "name": "@natal-chart/worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker for geocoding proxy and bot protection",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20231121.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.0",
    "wrangler": "^4.77.0"
  }
}